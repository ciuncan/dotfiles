# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
font pango:Audimat Mono 10
# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
bindsym $mod+p exec dmenu_run

# start file manager nautilus
bindsym $mod+o exec nautilus

# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left   focus left
bindsym $mod+Down   focus down
bindsym $mod+Up     focus up
bindsym $mod+Right  focus right

# move focused window
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+h move left
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left   move left
bindsym $mod+Shift+Down   move down
bindsym $mod+Shift+Up     move up
bindsym $mod+Shift+Right  move right

# split in horizontal orientation
bindsym $mod+v split h

# split in vertical orientation
bindsym $mod+s split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+w layout stacking
bindsym $mod+t layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# toggle fullscreen
bindsym $mod+button5 fullscreen

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# toggle keyboard layouts
bindsym $mod+Control+space exec /bin/sh ~/.i3/toggle-xkbmap.sh
bindsym XF86Mail           exec /bin/sh ~/.i3/toggle-xkbmap.sh

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
bindsym $mod+d focus child

# switch to workspace
bindsym $mod+1 workspace 1:web
bindsym $mod+2 workspace 2:term
bindsym $mod+3 workspace 3:dev
bindsym $mod+4 workspace 4:db
bindsym $mod+5 workspace 5:comm
bindsym $mod+6 workspace 6:fun; layout tabbed
bindsym $mod+7 workspace 7:vid
bindsym $mod+8 workspace 8:torr
bindsym $mod+9 workspace 9:img
bindsym $mod+0 workspace 10:aud
bindsym $mod+bracketleft  workspace prev
bindsym $mod+bracketright workspace next
bindsym $mod+grave        workspace next

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace 1:web,   workspace 1:web
bindsym $mod+Shift+2 move container to workspace 2:term,  workspace 2:term
bindsym $mod+Shift+3 move container to workspace 3:dev,   workspace 3:dev
bindsym $mod+Shift+4 move container to workspace 4:db,    workspace 4:db
bindsym $mod+Shift+5 move container to workspace 5:comm,  workspace 5:comm
bindsym $mod+Shift+6 move container to workspace 6:fun,   workspace 6:fun
bindsym $mod+Shift+7 move container to workspace 7:vid,   workspace 7:vid
bindsym $mod+Shift+8 move container to workspace 8:torr,  workspace 8:torr
bindsym $mod+Shift+9 move container to workspace 9:img,   workspace 9:img
bindsym $mod+Shift+0 move container to workspace 10:aud,  workspace 10:aud
bindsym $mod+Shift+braceleft move container to workspace prev,  workspace prev
bindsym $mod+Shift+braceright move container to workspace next, workspace next
bindsym $mod+Shift+grave move container to workspace next,      workspace next


# workspace assignment (use "xprop")
assign [class="(?i)google-chrome" window_role="browser"] 1:web
assign [class="(?i)firefox"] 1:web

assign [class="(?f)ilezilla"] 2:term

assign [class="(?i)jetbrains"] 3:dev
assign [class="^[gG]vim$"] 3:dev
assign [class="^[gG]it-gui$"] 3:dev
assign [class="^(?i)gitk$"] 3:dev

assign [class="^[pP]gadmin3$"] 4:db
assign [class="^(?i)virt-manager$"] 4:db
#assign [class="^URxvt$" instance="^mail$"] 4

assign [class="(?i)thunderbird"] 5:comm
assign [class="(?i)pidgin"] 5:comm
assign [class="(?i)google-chrome" window_role="app"] 5:comm
#assign [class="^crx_knipolnnllmklapflnccelgolnpehhpl$"] 5:comm 
assign [instance="^(?i)Hangouts$"] 5:comm
assign [title="^(?i)Hangouts$"] 5:comm
assign [class="^TeamViewer.exe$"] 5:comm
assign [title="^TeamViewer$"] 5:comm
assign [title="^Computers & Contacts$"] 5:comm
assign [title="- TeamViewer -"] 5:comm
assign [class="^ts3client_linux_amd64$"] 5:comm

assign [class="^Steam$"] 6:fun
assign [title="^Steam$"] 6:fun
assign [class="World of Warcraft"] 6:fun
assign [class="(?i)wow"] 6:fun
assign [class="(?i)battle.net"] 6:fun
assign [class="^(?i)explorer.exe$"] 6:fun
assign [title="^Wine System Tray$"] 6:fun

assign [class="^Video$"] 7:vid
assign [class="^[Vv]lc$"] 7:vid

assign [class="^(?i)transmission-gtk$"] 8:torr
assign [class="^(?i)deluge$"] 8:torr
assign [class="^(?i)gimp$"] 9:img

# custom window settings
for_window [class="^XCalc$"] floating enable
for_window [class="^URxvt$" instance="scratchpad"] border pixel 2; move scratchpad
for_window [class="(?i)google-chrome"] border pixel 2
for_window [class="(?i)firefox"] border pixel 2
for_window [class="(?i)thunderbird"] border pixel 2
for_window [class="(?i)jetbrains"] border pixel 2
for_window [class="(?i)intellij"] border pixel 2

for_window [class="^GVim$"] split v, layout stacking
for_window [class="^TeamViewer.exe$"] floating enable
for_window [title="^TeamViewer$"] floating enable
for_window [title="^Computers & Contacts$"] floating enable
for_window [title="- TeamViewer -"] floating enable
for_window [class="^Steam$"] floating enable; border none
for_window [class="^feh$"] fullscreen
for_window [class="^Meld$"] fullscreen
for_window [class="^MPlayer$"] border none; fullscreen
for_window [class="^Vlc$"] border none #; fullscreen
for_window [class="^[gG]it-gui$"] floating enable
for_window [class="^(?i)gitk$"] floating enable
for_window [class="^(?i)virt-manager$"] floating enable
## for_window [class="(?i)battle.net"] floating enable
for_window [class="^crx_knipolnnllmklapflnccelgolnpehhp$"] floating enable
# for_window [instance="^(?i)Hangouts$"] border none; fullscreen;
# for_window [title="^(?i)Hangouts$"] border none; fullscreen;
for_window [window_role="pop-up"] floating enable
for_window [window_role="Preferences"] floating enable
for_window [window_role="About"] floating enable

# Back and forth
workspace_auto_back_and_forth yes

bindsym $mod+z workspace back_and_forth
bindsym $mod+Shift+z move container to workspace back_and_forth; workspace back_and_forth

# Multimedia bindings
bindsym XF86AudioRaiseVolume  exec pactl set-sink-volume 1 +5%
bindsym XF86AudioLowerVolume  exec pactl set-sink-volume 1 -- -5%
bindsym XF86MonBrightnessUp   exec xbacklight +5%
bindsym XF86MonBrightnessDown exec xbacklight -5%
bindsym XF86AudioMute exec pactl set-sink-mute 1 toggle
bindsym XF86AudioPlay exec ncmpcpp toggle
bindsym XF86AudioStop exec ncmpcpp stop
bindsym XF86AudioPrev exec ncmpcpp prev
bindsym XF86AudioNext exec ncmpcpp next

bindsym XF86HomePage exec nautilus

bindsym Print exec gnome-screenshot -w

# lock screen, black background
bindsym $mod+Control+l exec "i3lock -c 000000 -d -n"
# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
bindsym $mod+Control+Shift+x exit
# exit i3 (logs you out of your X session)
#bindsym $mod+Control+Shift+x exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

mode  "(s)leep (h)ybernate (p)owersave (r)estart shut(d)own" {

    bindsym s exec dbus-send --system --print-reply --dest="org.freedesktop.UPower" /org/freedesktop/UPower org.freedesktop.UPower.Suspend; mode "default"
    bindsym h exec dbus-send --system --print-reply --dest="org.freedesktop.UPower" /org/freedesktop/UPower org.freedesktop.UPower.Hibernate; mode "default"
    bindsym p exec sudo pm-powersave; mode "default"
    bindsym d exec dbus-send --system --print-reply --dest="org.freedesktop.ConsoleKit" /org/freedesktop/ConsoleKit/Manager org.freedesktop.ConsoleKit.Manager.Stop; mode "default"
    bindsym r exec dbus-send --system --print-reply --dest="org.freedesktop.ConsoleKit" /org/freedesktop/ConsoleKit/Manager org.freedesktop.ConsoleKit.Manager.Restart; mode "default"

    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+n mode "(s)leep (h)ybernate (p)owersave (r)estart shut(d)own"

# resize window (you can also use the mouse for that)
mode "resize" {
# These bindings trigger as soon as you enter the resize mode

# Pressing left will shrink the window’s width.
# Pressing right will grow the window’s width.
# Pressing up will shrink the window’s height.
# Pressing down will grow the window’s height.
    bindsym h resize shrink width   2 px or 2 ppt
    bindsym j resize grow height    2 px or 2 ppt
    bindsym k resize shrink height  2 px or 2 ppt
    bindsym l resize grow width     2 px or 2 ppt
    bindsym Shift+h resize shrink width  10 px or 10 ppt
    bindsym Shift+j resize grow height   10 px or 10 ppt
    bindsym Shift+k resize shrink height 10 px or 10 ppt
    bindsym Shift+l resize grow width    10 px or 10 ppt

# same bindings, but for the arrow keys
    bindsym Left resize shrink width  2 px or 2 ppt
    bindsym Down resize grow height   2 px or 2 ppt
    bindsym Up resize shrink height   2 px or 2 ppt
    bindsym Right resize grow width   2 px or 2 ppt
    bindsym Shift+Left resize shrink width  10 px or 10 ppt
    bindsym Shift+Down resize grow height   10 px or 10 ppt
    bindsym Shift+Up resize shrink height   10 px or 10 ppt
    bindsym Shift+Right resize grow width   10 px or 10 ppt

# back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

mode "mark_window" {
    bindsym 1 mark m1
    bindsym 2 mark m2
    bindsym 3 mark m3
    bindsym 4 mark m4
    bindsym 5 mark m5
    bindsym a mark ma
    bindsym b mark mb
    bindsym c mark mc
    bindsym d mark md
    bindsym e mark me

    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+m mode "mark_window"

mode "go_to_window" {
    bindsym 1 [con_mark="m1"] focus
    bindsym 2 [con_mark="m2"] focus
    bindsym 3 [con_mark="m3"] focus
    bindsym 4 [con_mark="m4"] focus
    bindsym 5 [con_mark="m5"] focus
    bindsym a [con_mark="ma"] focus
    bindsym b [con_mark="mb"] focus
    bindsym c [con_mark="mc"] focus
    bindsym d [con_mark="md"] focus
    bindsym e [con_mark="me"] focus

    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+g mode "go_to_window"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
    #tray_output primary
    #status_command ~/.i3/status_command.sh #i3status
    status_command ~/.i3/conky-i3bar-wrapper.sh
    colors {
        # solarized
        background #073642
        statusline #eee8d5
        focused_workspace   #cb4b16 #cb4b16 #eee8d5
        active_workspace    #cb4b16 #cb4b16 #eee8d5
        inactive_workspace  #b58900 #b58900 #eee8d5
    }
}


# class                 border  backgr. text    indicator
client.focused          #859900 #859900 #fdf6e3 #2e9ef4
client.focused_inactive #268bd2 #268bd2 #fdf6e3 #484e50
client.unfocused        #333333 #93a1a1 #fdf6e3 #292d2e

# Startup
exec --no-startup-id compton -bc
exec --no-startup-id firefox #google-chrome
exec --no-startup-id thunderbird
exec --no-startup-id pidgin
exec --no-startup-id i3-msg 'workspace 2:term; exec gnome-terminal; exec nautilus; workspace 1:web'
#exec --no-startup-id transmission-gtk
#exec --no-startup-id deluge
exec --no-startup-id redshift
exec --no-startup-id nm-applet
exec --no-startup-id keepassx
exec --no-startup-id dropbox start
#exec --no-startup-id ibus-daemon --xim -d -r
exec --no-startup-id feh --bg-fill --recursive --randomize $HOME/Pictures/wallpaper/*
# exec --no-startup-id exec mpd
exec gsettings set org.gnome.desktop.background show-desktop-icons false
# disable touchpad on keyboard activity
exec --no-startup-id syndaemon -i 2 -d -t -K
exec --no-startup-id xautolock -time 5 -locker "i3lock -c 000000 -d -n" 
exec --no-startup-id synapse

#exec --no-startup-id battlenet
#exec --no-startup-id steam

exec --no-startup-id ~/.i3/autostart
exec --no-startup-id ~/.i3/i3batwarn.sh
